using Algorithms;

class Program
{
    public static void Main(string[] args)
    {
        // Reverse String
        var s = new char[] { 'h', 'e', 'l', 'l', 'o' };
        ReverseStringSolution.ReverseString(s);
    }
}